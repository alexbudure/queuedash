---
"@queuedash/api": major
"@queuedash/client": major
"@queuedash/ui": major
---

Add the Queuedash v4 configuration foundation: server-provided branding and UI
defaults, instance-scoped browser settings, centrally enforced data redaction,
cached opt-in Redis queue discovery, bounded job search, worker inspection,
scoped Tailwind output, instance-scoped dashboard preferences, server-enforced
queue access rules, whole-category privacy controls, server policy visibility,
hidden-policy non-disclosure, correctly cased public API aliases, and focused
package documentation for the new configuration surface. Replace the default
browser-native Basic Auth challenge with a branded, white-label-aware login,
signed HttpOnly sessions, explicit logout, expiry handling, Docker session
configuration, and an opt-in legacy Basic Auth mode.
